# PostgreSQL connection (used by Docker and/or backend)
POSTGRES_USER=your_prod_user
POSTGRES_PASSWORD=your_secure_password
POSTGRES_DB=your_prod_db

# Optional for Kubernetes – only needed if backend uses separate DB_HOST/DB_PORT instead of DATABASE_URL
DB_HOST=db
DB_PORT=5432
